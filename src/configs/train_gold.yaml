# A4 对照：全参微调（金标多任务主路径）
model_name: models/chinese-bert-wwm-ext
lstm_hidden: 256
lstm_layers: 1
dropout: 0.1

use_lora: false

batch_size: 8
grad_accum_steps: 2
fp16: true
epochs: 3
lr: 2.0e-5
weight_decay: 0.01
seed: 42
max_steps: 0

benzi_loss_weight: 1.0
lexicon_filter: true

out_dir: checkpoints/gold_full
